accessing
defaultContainerPolicy
	"Answers the policy to chose container used by default"
	
	"OmniBase has a default container and this policy will 
	decide to send ot it every new persistent object."
	^ ARDefaultContainer 