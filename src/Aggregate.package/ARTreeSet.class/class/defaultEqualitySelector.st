accessing
defaultEqualitySelector
	"Answers the default selector to use for the equality check."
	
	"#isIdenticalTo: is about identity. It's used by OmniBase as #== but for persistent objects."
	^ #isIdenticalTo: